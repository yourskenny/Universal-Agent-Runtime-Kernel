这是用于测试 V7 智能体文档学习能力的测试文件。
MCP (Model Context Protocol) 是一种让 AI 模型连接外部数据的标准协议。
通过 MCP，我们可以让本地智能体读取 PDF、Word 等各种格式的文件。
当智能体读取文件后，它可以将内容存入向量数据库 (ChromaDB)，从而实现“学习”的效果。
这意味着，即使是一个离线的小模型，也可以通过这种方式拥有海量的专业知识。
